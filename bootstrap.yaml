
http:
  host: localhost
  port: 23333


discover:
  Host: localhost
  port: 30001
  instanceId: gateway-service-localhost
  serviceName: gateway


config:
  id: config-service
  profile: "dev"
  label: "master"

trace:
  host: localhost
  port: 30004
  url: /api/v2/spans

rpc:
  port: 1111

auth:
  permitAll:
    -
      /oauth/**
    -
      /string/**

